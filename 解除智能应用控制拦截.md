# exe 被「智能应用控制 / Defender」拦截怎么办？

## 先做这一步（本机最有效）

**右键 `安装信任并放行.bat` → 选「是」允许管理员权限。**

它会自动：

1. 用本机证书 **签名主程序 + `_internal` 里所有 dll/pyd**  
2. 把证书装进 **本机「受信任的根证书 / 受信任的发布者」**  
3. 给本文件夹加 **Windows Defender 排除**（避免 PyInstaller 被当 PUA 误杀）  
4. **关闭智能应用控制**（若系统策略允许）  
5. 尝试直接启动 exe  

若刚从「强制」模式改过来，**重启一次**再双击 `保险报告生成器.exe`。

---

## 为什么「以前版本能开、现在不行」？

本机策略里曾出现过：

- 智能应用控制 **曾经是强制（2）**，后来变成评估（1）  
- Defender **PUA 防护开启** — 新打包的 PyInstaller 程序经常被拦  

每次重新打包，**文件哈希变了**，以前的「放过记录」不会继承到新 exe，所以会感觉「旧版没事、新版被拦」。

---

## 自签能做什么、不能做什么？

| 做法 | 效果 |
|------|------|
| 本机自签 + 信任证书 + 全量签 dll | **本机**通常可放行 |
| Defender 排除本目录 | 减少 PUA 误杀 |
| 关闭智能应用控制 | 本机不再拦无信誉软件 |
| 自签拿到「微软互联网信誉」 | **做不到**（需商业证书 / Azure Trusted Signing） |
| 拷到别人电脑强制 SAC 环境直接跑 | 仍可能被拦 |

多机分发请购买 **代码签名证书** 或使用 **Azure Trusted Signing**。

---

## 备用启动（不依赖 exe）

双击 **`启动报告生成器.bat`**（走 Python，一般不被当未知 exe 拦）。

---

## 手动关闭智能应用控制

Windows 安全中心 → 应用和浏览器控制 → 智能应用控制设置 → **关闭**
